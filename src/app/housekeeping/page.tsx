import { HousekeepingManager } from "@/components/ops/HousekeepingManager";

export const metadata = { title: "Housekeeping" };

export default function HousekeepingPage() {
  return <HousekeepingManager />;
}
