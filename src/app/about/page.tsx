import { PublicAboutPage } from "@/components/site/PublicAboutPage";

export const metadata = {
  title: "About · Mistnleaf",
  description: "Our story — a quiet forest retreat in Munnar.",
};

export default function AboutPage() {
  return <PublicAboutPage />;
}
